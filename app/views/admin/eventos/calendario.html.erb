<%= render 'menu' %>

<%= month_calendar events: @eventos do |date, eventos| %>
  <%= date.day %>
  <%= eventos.count %>

  <% eventos.each do |evento| %>
    <div>
      <%= evento.nome %>
    </div>
  <% end %>
<% end %>
