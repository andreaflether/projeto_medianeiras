<h1 class="h1-evt">Próximos eventos</h1>
<br>

<div class="container">

  <h3 class="admin-title-evt"><b><%= l Date.today, format: :month_only %></b></h3>

  <% if @proximos_eventos.blank? %>
    <h6 class="mt-3">Não há eventos agendados para este mês.<h6>
  <% end %>

  <% @proximos_eventos.order(data: :asc, hora: :asc).each do |evento| %>
      <div class="mb-4 mt-3">
        <div class="card-event my-auto" style="background-color: #f0f0f0; padding: 20px 20px; border-left: 5px solid #007bff;">
          <h4 class="align-middle"><b><%= evento.data.day %></b> - <%= evento.nome %> - <%= evento.hora %></h4>
          <p class="align-middle"><i class="fas fa-map-marker-alt text-primary mr-2"></i><%= evento.local %> - <%= evento.descricao %></p>
          <%= link_to 'Editar', edit_evento_path(evento), class: 'btn btn-outline-primary btn-next' %>
          <%= link_to 'Apagar', evento_path(evento), class: 'btn btn-outline-danger btn-next', method: :delete, data: { confirm: 'Tem certeza que deseja apagar o evento selecionado?' } %>
        </div>
      </div>
  <% end %>
</div>

<%#= l evento.data, format: :short %>
