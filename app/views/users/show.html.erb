<h1 class="h1-evt">Detalhes de usuário</h1>
<h4><%= @user.name %></h4>
<h5><%= @user.email %></h5>

<div class="card">
  <div class="card-header">
    <b><%= @user.name %></b> (<%= @user.email %>)
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item"><%= @user.role.capitalize %></li>
    <li class="list-group-item"><%= l @user.created_at, format: :long%></li>
    <li class="list-group-item"><%= @user.updated_at %></li>
  </ul>
</div>
